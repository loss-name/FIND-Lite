# Contributing

The default FIND-Lite algorithm is a frozen research baseline for version 0.1.0.
Please keep benchmark changes, algorithm changes, and documentation changes separate.
Any algorithm modification must receive a new version name and an independent ablation
protocol. Reproducibility reports should include Python, NumPy, dimensions, budget,
seeds, and the exact method parameters.
