# 阶段一：边界定位

状态：已完成。

冻结对象为 `src/find_lite.py` 的默认 `find_lite`。每个完成的扫描文件必须写明维度、预算
倍率、函数、10个固定种子和完整分位统计。未完成全矩阵前，不对“最高可用维度”或“最大
有效预算”作结论。

阶段一已选出并完成阶段二的五个代表点；正式对照汇总见 `stage2_confirmation_summary.json` 与 `../docs/PERFORMANCE_POSITIONING.md`。
