# 阶段三：未见函数与对照敏感性

函数集在此前算法设计和消融中未使用：Zakharov、Griewank、Levy、Schwefel、旋转 Rastrigin。
默认维度10、30，预算`1000d`，20个固定种子8000--8019。

比较冻结 Lite、两个 DE 参数组（`F=0.5/CR=0.9`与`F=0.8/CR=0.5`）及两个 PSO 参数组
（`w=0.7298,c1=c2=1.49618`与`w=0.7,c1=c2=1.5`）。参数组只用于检验“结论是否依赖
单一对照设定”，不构成全面参数调优。
