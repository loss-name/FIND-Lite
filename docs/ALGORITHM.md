# Algorithm`n`nFIND-Lite uses two evaluated population points as geometric anchors. A candidate is generated on their line, clipped to box constraints, evaluated, and accepted when it improves the incumbent. The implementation intentionally keeps the core mechanism small so ablations remain interpretable.`n`nThe algorithm is a local line-search heuristic, not a proof of global convergence.
